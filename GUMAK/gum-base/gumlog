Usage: gum log <text> ...

Log messages to output

Arguments:
  <text> ...    Text to log

Flags:
  -h, --help                Show context-sensitive help.
  -v, --version             Print the version number

  -o, --file=STRING         Log to file
  -f, --format              Format message using printf
      --formatter="text"    The log formatter to use
  -l, --level="none"        The log level to use
      --prefix=STRING       Prefix to print before the message
  -s, --structured          Use structured logging
  -t, --time=""             The time format to use (kitchen, layout, ansic,
                            rfc822, etc...)

Style Flags
  --level.foreground=""        Foreground Color ($GUM_LOG_LEVEL_FOREGROUND)
  --time.foreground=""         Foreground Color ($GUM_LOG_TIME_FOREGROUND)
  --prefix.foreground=""       Foreground Color ($GUM_LOG_PREFIX_FOREGROUND)
  --message.foreground=""      Foreground Color ($GUM_LOG_MESSAGE_FOREGROUND)
  --key.foreground=""          Foreground Color ($GUM_LOG_KEY_FOREGROUND)
  --value.foreground=""        Foreground Color ($GUM_LOG_VALUE_FOREGROUND)
  --separator.foreground=""    Foreground Color ($GUM_LOG_SEPARATOR_FOREGROUND)


-o --file=STRING      Log to file
-f --format           Format message using printf
--formatter=\"text\"  The log formatter to use
-l --level=\"none\"   The log level to use
--prefix=STRING       Prefix to print before the message
-s --structured       Use structured logging
-t --time=\"\"        The time format to use (kitchen, layout, ansic, rfc822, etc...)
--level.foreground=\"\"      Foreground Color (\$GUM_LOG_LEVEL_FOREGROUND)
--time.foreground=\"\"       Foreground Color (\$GUM_LOG_TIME_FOREGROUND)
--prefix.foreground=\"\"     Foreground Color (\$GUM_LOG_PREFIX_FOREGROUND)
--message.foreground=\"\"    Foreground Color (\$GUM_LOG_MESSAGE_FOREGROUND)
--key.foreground=\"\"        Foreground Color (\$GUM_LOG_KEY_FOREGROUND)
--value.foreground=\"\"      Foreground Color (\$GUM_LOG_VALUE_FOREGROUND)
--separator.foreground=\"\"  Foreground Color (\$GUM_LOG_SEPARATOR_FOREGROUND)
